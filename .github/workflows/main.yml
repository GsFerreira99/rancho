name: Deploy

on:
  push:
    branches: main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-18.04
    environment: 
      name: production
      url: https://ranchoalere.com

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Install Dependencies
      run: |
        sudo apt-get update
        source /sistemas/rancho/venv/bin/activate
        python -m pip install --upgrade pip
        pip install -r /home/rancho/sistemas/rancho/rancho/requirements.txt
        python manage.py migrate
        python manage.py collectstatic
        sudo systemctl restart gunicorn
        sudo systemctl daemon-reload
        sudo systemctl restart gunicorn.socket gunicorn.service
        sudo nginx -t && sudo systemctl restart nginx
    
    - name: 📂 Sync files
      uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      with:
            username: rancho
            server: ranchoalere.com
            port: 22
            remote_path: /home/rancho/sistemas/rancho/
            sftp_only: true
            password: Gabriel151299

          
      
